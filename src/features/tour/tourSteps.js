/* The walkthrough is the demo now (src/demo/). This key is the one thing the
   two share: `pw-tour` in progress, set once the walkthrough has been seen, so
   it starts by itself only for a student who has not. */
export const TOUR_KEY = "pw-tour";
